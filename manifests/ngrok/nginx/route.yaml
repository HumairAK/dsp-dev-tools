kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: nginx-kfp-http
  namespace: kubeflow
  labels:
    app: ml-pipeline-ui
spec:
  to:
    kind: Service
    name: ml-pipeline-ui
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None